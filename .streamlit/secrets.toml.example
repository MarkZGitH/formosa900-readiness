# 把这个文件复制为 secrets.toml 并填入你的API key
# 注意：secrets.toml 不会进git (已在.gitignore中)

OPENROUTER_API_KEY = "sk-or-v1-xxxxxxxxxxxxxxxxxxxxxxxx"
DEFAULT_VISION_MODEL = "google/gemini-2.5-flash"
DEFAULT_COACH_MODEL = "google/gemini-2.5-flash"
